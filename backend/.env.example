# 데이터베이스 (Supabase 또는 로컬 PostgreSQL)
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/muknoljam

# Claude AI API 키 (https://console.anthropic.com 에서 발급)
ANTHROPIC_API_KEY=sk-ant-...

# JWT 시크릿 (운영 환경에서는 반드시 랜덤 문자열로 변경)
SECRET_KEY=change-me-in-production-use-random-string

# CORS 허용 Origins
CORS_ORIGINS=["http://localhost:3000"]
